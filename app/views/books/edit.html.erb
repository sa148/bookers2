<h2>Editing Book</h2>
<% if @book.errors.any? %>
<div class="error_explanation">
  <h2><%=@book.errors.count %>
  errors prohibited this obj from being saved:</h2>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
<%= form_with model: @book do |f| %>
<div class="field">
  <%= f.label :title %><br>
  <%= f.text_field :title %>
</div>
<div class="field">
  <%= f.label :opnion %><br>
  <%= f.text_area :body %>
</div>
  <%= f.submit 'Editing Book', class: "btn btn-success" %>
<% end %>
<div class="edit-sb">
  <%= link_to "Show", book_path(@book.id)%>
|
  <%= link_to "Back", books_path%>
</div>
