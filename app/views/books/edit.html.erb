<div class="flash">You have updated book successfully.</div>
<h2>Editing Book</h2>
<%= form_with model: @book do |f| %>
<div class="field">
  <%= f.label :title %><br>
  <%= f.text_field :title %>
</div>
<div class="field">
  <%= f.label :opnion %><br>
  <%= f.text_area :body %>
</div>
  <%= f.submit 'Editing Book', class: "btn btn-secondary" %>
<% end %>
<div class="edit-sb">
  <%= link_to "Show", book_path(@book.id)%>
|
  <%= link_to "Back", books_path%>
</div>
