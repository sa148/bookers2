<h2>Editing Book</h2>
<%= form_with model: @book do |f| %>
<div class="field">
  <%= f.text_field :title %>
</div>
<div class="field">
  <%= f.text_area :Opnion %>
</div>
  <%= f.submit 'Update Book' %>
  <div class="edit-sb">
  <%= link_to "Show", book_path(@book.id)%>
|
  <%= link_to "Back", books_path%>
</div>
<% end %>